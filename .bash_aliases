# SSH ALIASES
alias sshcs="ssh -Y userid@school"

# KEYBINDINGS
alias u="cd .."
alias uu="cd ../.."
alias uuu="cd ../../.."

alias mv='mv -v' # verbose flag, indicates which file moves where
alias cp='cp -v'
alias rm='rm -v'

alias dc="docker-compose"
alias dc-logs="docker-compose logs -f"

alias df="df -h"
alias du="du -h"

alias ls='ls --color'
alias ll='ls -lahG'

alias vim='nvim'

alias ga="git add"
alias gd="git diff"
alias gs="git status"

alias rc="vim ~/.bash_aliases"  # rc for bashrc, but aliases instead

alias g++20="g++ --std=c++20"

# DIRECTORY SHORTCUTS
